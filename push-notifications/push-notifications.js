module.exports = function(core) {
	core.on("text", function(){
		// generate the notification objects.
		// get the list of registration-id or device-id for the devices to which tis has to be sent.
		//shoot
	}, "watcher");
};



function textHandler(action, next) {
	
	next();
}